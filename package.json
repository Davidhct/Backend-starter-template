{
  "name": "beckend-tutorial",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "yarn build && node build/server.js",
    "start:dev": "ts-node src/server.ts",
    "start:dev:mon": "yarn nodemon",
    "build": "rimraf ./build && tsc"
  },
  "dependencies": {
    "@types/node": "^20.4.10",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
